import { Startups } from 'app/entities/startups/startups.model';

export class PlanesInversion {
  constructor(public id: number, public nombre: string, public monto: number, public descripcion: string, public beneficios: string) {}
}
